[package]
name        = "block-abi"
version     = "0.1.0"
edition      = "2024"
rust-version = "1.92"
license     = "Apache-2.0"
description = "Block-device IPC ABI shared across virtio-blk / ahci / nvme / ide drivers."

[lib]
name = "block_abi"
path = "lib.rs"

[dependencies]
zigbone-abi = { git = "https://github.com/crux-os/zigbone-abi.git" }
